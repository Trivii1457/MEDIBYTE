@model AdministracionHonorariosServiciosModel 
@{
    string Prefix = "AdministracionHonorariosServicios"; 
}

<script>

    function @(Prefix)OnValueChangedTipoPagoParticularEstadosId(e)
    {
        if (e.value == 77) {
            $("#@(Prefix)ValorHonorarioParticular").dxNumberBox("instance").option("max", 50000000);
        } else {
            $("#@(Prefix)ValorHonorarioParticular").dxNumberBox("instance").option("max", 100);
        }
    }

    function @(Prefix)OnValueChangedTipoPagoConvenioEstadosId(e)
    {
        if (e.value == 77) {
            $("#@(Prefix)ValorHonorarioConvenio").dxNumberBox("instance").option("max", 50000000);
        } else {
            $("#@(Prefix)ValorHonorarioConvenio").dxNumberBox("instance").option("max", 100);
        }
    }

    function @(Prefix)OnValueChangedServiciosId(e)
    {
        if (e.value == 0 || e.value == "" || e.value == null) {
            $("#@(Prefix)TipoPagoConvenioEstadosId").dxSelectBox("instance").option("readOnly", true);
            $("#@(Prefix)TipoPagoParticularEstadosId").dxSelectBox("instance").option("readOnly", true);
            $("#@(Prefix)ValorHonorarioConvenio").dxNumberBox("instance").option("readOnly", true);
            $("#@(Prefix)ValorHonorarioParticular").dxNumberBox("instance").option("readOnly", true);
            $("#@(Prefix)TipoPagoConvenioEstadosId").dxSelectBox("instance").option("value", null);
            $("#@(Prefix)TipoPagoParticularEstadosId").dxSelectBox("instance").option("value", null);
            $("#@(Prefix)ValorHonorarioConvenio").dxNumberBox("instance").option("value", null);
            $("#@(Prefix)ValorHonorarioParticular").dxNumberBox("instance").option("value", null);

        } else {
            $("#@(Prefix)TipoPagoConvenioEstadosId").dxSelectBox("instance").option("readOnly", false);
            $("#@(Prefix)TipoPagoParticularEstadosId").dxSelectBox("instance").option("readOnly", false);
            $("#@(Prefix)ValorHonorarioConvenio").dxNumberBox("instance").option("readOnly", false);
            $("#@(Prefix)ValorHonorarioParticular").dxNumberBox("instance").option("readOnly", false);

        }
    }

    function @(Prefix)FormSuccess(data)
    {
        hideLoadIndicator();
        if ('@ViewData.ModelState.IsValid' == 'True')
        {
            if ("@ViewBag.Accion" == "Delete") {
                DevExpress.ui.notify("Registro eliminado correctamente.","info");
            } else if ("@ViewBag.Accion" == "Save") {
                DevExpress.ui.notify("Registro guardado correctamente.","info");
            } else {
                DevExpress.ui.notify("Error no accion en submit.","info");
            }
        }
    }

    function @(Prefix)FormFailure(data)
    {
        hideLoadIndicator();
        DevExpress.ui.notify("Error en el formulario","error");
    }

    function @(Prefix)FormSuccessDetail(data)
    {
        hideLoadIndicator();
        if ('@ViewData.ModelState.IsValid' == 'True')
        {
            $("#AdministracionHonorariosServiciosDetalleDataGrid").dxDataGrid("instance").refresh();
            if ("@ViewBag.Accion" == "Delete") {
                DevExpress.ui.notify("Registro eliminado correctamente.","info");
            } else if ("@ViewBag.Accion" == "Save") {
                DevExpress.ui.notify("Registro guardado correctamente.","info");
            } else {
                DevExpress.ui.notify("Error no accion en submit.","info");
            }
        }
    }

    function @(Prefix)FormFailureDetail(data)
    {
        hideLoadIndicator();
        DevExpress.ui.notify("Error en el formulario","error");
    }

</script>
